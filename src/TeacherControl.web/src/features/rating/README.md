# F2 Rating (referenční featura)

Vlastník: _Učený_

Tahle featura je vzor pro ostatní. Ukazuje najednou: TanStack Query v `api.ts`, query key
`['rating', 'list', filtry]`, invalidaci po mutaci, loading a error stav, formulář přes React Hook
Form + Zod (`z.infer`), oprávnění přes `hasRole()` a MSW handlery v `mocks.ts`.
