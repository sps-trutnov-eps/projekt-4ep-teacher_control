# Teacher Control

Údaje pro databázi a testovacího uživatele. Databázi lze lokálně vyřešit pomocí db-only.compose.yaml.

- Database: `localhost:5432`, database/user `control`.
- Development login: `test@example.com` / `TeacherControl123`.
[.env](.env)